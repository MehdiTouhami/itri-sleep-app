[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt && python backend/ingest.py && python backend/ingest_research.py"

[deploy]
startCommand = "bash -c 'cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "on_failure"
